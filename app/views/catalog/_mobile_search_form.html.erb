<div id="moblet-search-bar" class="visible-phone">
  <div class="container">
  <%= form_tag search_action_url, :method => :get, :class => 'form-inline search-form' do %>
    <%= search_as_hidden_fields(:omit_keys => [:q, :search_field, :qt, :page]).html_safe %>        
    <div class="input-append">
      <%= text_field_tag :q, params[:q], :class => "q input-medium search-query" %><btn class='btn'><i type='submit' class='icon-search icon'></i></btn>
    </div>
    <% unless search_fields.empty? %>
      <%= label_tag(:search_field, " #{t('blacklight.search.form.search_field.label')} ") %>
      <%= select_tag(:search_field, options_for_select(search_fields, h(params[:search_field])), :title => t('blacklight.search.form.search_field.title'), :class=>"search_field input-small") %>
    <% end %>
  <% end %>
  </div>
</div>

